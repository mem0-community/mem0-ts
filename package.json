{
  "private": true,
  "packageManager": "pnpm@10.5.2",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "prettier": "^3.5.2",
    "tsup": "^8.4.0",
    "typescript": "~5.5.4",
    "vitest": "^3.0.0"
  }
}
